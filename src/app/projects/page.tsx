import { WEB_INFO } from '@/core/util/constants'

const ProjectsPage = () => {
  return <div>Projects</div>
}

export default ProjectsPage

export const metadata = {
  title: WEB_INFO().PROJECTS_PAGE.TITLE,
  description: WEB_INFO().PROJECTS_PAGE.DESC,
}
