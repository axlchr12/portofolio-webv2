import { WEB_INFO } from '@/core/util/constants';

const ContactPage = () => {
  return <div>Contact</div>;
};

export default ContactPage;

export const metadata = {
  title: WEB_INFO().CONTACT_PAGE.TITLE,
  description: WEB_INFO().CONTACT_PAGE.DESC,
};
